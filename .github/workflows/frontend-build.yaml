name: "Frontend Build"

on:
  push:
    paths:
      - "studio/**"
  pull_request:
    paths:
      - "studio/**"
  workflow_dispatch:

concurrency:
  group: frontend-build-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  frontend-build:
    permissions:
      contents: write
    uses: pimcore/workflows-collection-public/.github/workflows/reusable-studio-frontend-build.yaml@d89284720b851e94f595d96e669d4be2961aeab6 # main
    with:
      working-directory: ./studio
      node-version: "26"
      build-output-path: ./public/build/production/
